{
  "name": "coffeelib",
  "bin": "./bin/coffeelib",
  "version": "0.0.0",
  "scripts": {
    "build": "coffee --compile --output lib src",
    "start": "coffee --watch --compile --output lib src",
    "test": "jasmine"
  },
  "dependencies": {
    "@ch1c0t/io": "^0.0.3",
    "@ch1c0t/sh": "^0.0.1",
    "chokidar": "^3.5.2"
  },
  "devDependencies": {
    "coffeescript": "^2.5.1",
    "jasmine": "^3.9.0"
  }
}
